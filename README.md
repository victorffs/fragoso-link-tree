# fragoso-link-tree
My own link tree

## Deploy (Netlify)

This is a static site. Netlify should publish the repository root (`.`).

- Build command: *(empty)*
- Publish directory: `.`
- Config file: `netlify.toml`
